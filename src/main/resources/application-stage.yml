server:
  port: 8081

spring:
  application:
    name: accelerate-dev-stage
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
