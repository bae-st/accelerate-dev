server:
  port: 8000

spring:
  application:
    name: accelerate-dev-dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
